var UImovie = (function () {
  var selectors = {
    selectorTitle: '#title',
    selectorLength: '#length',
    selectorGenre: '#genre',
    selectorButton: '#button',
    selectorCurrentArray: '#current-array',
    selectorError: '#error',
  };

  var inputTitle = document.querySelector (selectorTitle);
  var inputLength = document.querySelector (selectorLength);
  var inputGenre = document.querySelector (selectorGenre);
  var inputCurrentArray = document.querySelector (selectorCurrentArray);
  var inputError = document.querySelector (selectorError);
}) ();
